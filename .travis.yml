language: ruby
rvm: 2.2.0
script: "bin/rake spec"
before_script:
  - "bundle exec ruby $GEM_ROOT/gems/chromedriver-helper-1.0.0/bin/chromedriver-update"
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start" 
  - sleep 3 # give xvfb time to start
